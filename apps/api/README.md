# API app

Gateway server-side per provider di dati, persistenza e calcoli. Il token BSD è ammesso solo nel runtime server attraverso `.env.local`; non deve comparire in client, log, documentazione o commit.
